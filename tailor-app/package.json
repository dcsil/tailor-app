{
    "name": "tailor-app",
    "version": "1.0.0",
    "engines": {
      "node": "18.x",
      "npm": "9.x"
    },
    "scripts": {
      "heroku-postbuild": "cd frontend && npm install --include=dev --no-optional && ROLLUP_SKIP_LOAD_NATIVE_PLUGIN=1 npm run build"
    },
    "cacheDirectories": ["frontend/node_modules"]
}